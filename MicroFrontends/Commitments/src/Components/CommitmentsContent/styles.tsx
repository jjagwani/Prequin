import { makeStyles } from "@mui/styles";

export const useStyles: any = makeStyles(() => ({
    contentPadding: {
        padding: "20px"
    },
    loader: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
    }
}));