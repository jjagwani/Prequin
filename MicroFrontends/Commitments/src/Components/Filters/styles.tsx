import { makeStyles } from "@mui/styles";

export const useStyles: any = makeStyles(() => ({
    box: {
        display: "flex",
        gap: "10px",
        paddingTop: "10px"
      },
      chipBox: {
        display: "flex",
        flexDirection: "column",
        textAlign: "center",
      }
}));