import { makeStyles } from "@mui/styles";

export const useStyles: any = makeStyles(() => ({
    loader: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center"
    }
}));