import { makeStyles } from "@mui/styles";

export const useStyles: any = makeStyles(() => ({
  tableContainer: {
    maxHeight: "420px",
    overflow: "auto",
  },
  cursorPointer: {
    cursor: "pointer",
  },
  paddingTop: {
    paddingTop: "10px",
  }
}));